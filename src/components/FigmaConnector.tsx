"use client";

type Props = {
  onUrl: (url: string) => void;
};

export function FigmaConnector({ onUrl }: Props) {
  return (
    <div>
      {/* Figma URL input + validate button */}
    </div>
  );
}
