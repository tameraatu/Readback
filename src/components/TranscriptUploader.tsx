"use client";

type Props = {
  onTranscript: (text: string) => void;
};

export function TranscriptUploader({ onTranscript }: Props) {
  return (
    <div>
      {/* paste textarea + file upload (.txt, .docx, .pdf) */}
    </div>
  );
}
