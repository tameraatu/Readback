import type { Recommendation } from "@/types";

type Props = {
  recommendation: Recommendation;
};

export function RecommendationCard({ recommendation }: Props) {
  return (
    <div>
      {/* priority badge, suggestion, rationale, supporting quote with blue bar */}
    </div>
  );
}
